# InteractiveSvgMvp (AiVectra)

Deterministic SVG/debug artifact example for interactive vector UI.

## Run

```bash
ailang package restore
ailang build .
ailang run . snapshot
```

## Notes

- Imports AiVectra through the `aivectra` package.
- Useful for showing how agent-readable UI artifacts can match human-visible UI behavior.
