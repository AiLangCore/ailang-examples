# HelloName (AiVectra)

Text input, keyboard, click, and deterministic replay example for AiVectra.

## Run

```bash
ailang package restore
ailang build .
ailang run .
```

## Notes

- Imports AiVectra through the `aivectra` package.
- Intended as the first interactive UI demo after `hello-world`.
