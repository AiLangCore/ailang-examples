Program {
  Export(name=start)

  Let(name=start) {
    Fn(params=args) {
      Block {
        Call(target=sys.stdout.writeLine) { Lit(value="Hello from AiLang examples.") }
        Return { Lit(value=0) }
      }
    }
  }
}
